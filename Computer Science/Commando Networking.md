
**Se queda escuchando el server host**
```
PING [Host PATH]
```

**Se mira la coneccion con el server**
```
nslookup [host PATH]
```

**agregar DNS** para entornos unix
```
sudo nano /etc/hosts
```

```
nano ~/.zshrc
```

